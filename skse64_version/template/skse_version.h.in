// clang-format off
#pragma once

// version info

#define CURRENT_RELEASE_SKSE_STR "${VERSION}"

#define SKSE_VERSION_INTEGER ${VERSION_MAJOR}
#define SKSE_VERSION_INTEGER_MINOR ${VERSION_MINOR}
#define SKSE_VERSION_INTEGER_BETA ${VERSION_ALTER}

${define PACKED_SKSE_VERSION}

${define SKSE_VERSION_PADDEDSTRING}
${define SKSE_VERSION_RELEASEIDX}


// information about the state of the game at the time of release
${define RUNTIME_TYPE_BETHESDA}
${define RUNTIME_TYPE_GOG}
${define RUNTIME_TYPE_EPIC}

${define SKSE_TARGETING_BETA_VERSION}
${define CURRENT_RELEASE_RUNTIME}
${define SAVE_FOLDER_NAME}

#define MAKE_EXE_VERSION_EX(major, minor, build, sub)	((((major) & 0xFF) << 24) | (((minor) & 0xFF) << 16) | (((build) & 0xFFF) << 4) | ((sub) & 0xF))
#define MAKE_EXE_VERSION(major, minor, build)			MAKE_EXE_VERSION_EX(major, minor, build, 0)

// macro to get info from Packed Version (int)
#define GET_EXE_VERSION_MAJOR(a) (((a) & 0xFF000000) >> 24)
#define GET_EXE_VERSION_MINOR(a) (((a) & 0x00FF0000) >> 16)
#define GET_EXE_VERSION_BUILD(a) (((a) & 0x0000FFF0) >> 4)
#define GET_EXE_VERSION_SUB(a) (((a) & 0x0000000F) >> 0)


